# Project 1 - Simple Client
SimpleClient request server with NUID and keeps computing the equations until it gets an BYE message


To launch the program

```
$ ./simple_client <-p port> <-s> [hostname] [NEU ID]
```